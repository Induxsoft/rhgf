#include "dkli.dkh"
#!
module "var"
{
    @list_query="Select * from rh_variable Order by nombre;"
    @table_name="rh_variable"

    update::&db,&params,&data
    {
        do dbr.update(db,@table_name,data,"sys_pk=@_entity_id",params)
        return @null
    }
    point @update to update
}