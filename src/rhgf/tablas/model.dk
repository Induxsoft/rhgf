#include "dkli.dkh"
#!

module "model"
{
    @table_name = "rh_tabla_calculo"
    @list_query = "SELECT * FROM rh_tabla_calculo ORDER BY nombre"

    function tablas_delete(&db,&params)
    {
        do entity.delete(db,params)
        return @true
    }

    point @delete to tablas_delete
}